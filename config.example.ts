// Configuration file example
// Copy this file to config.ts and fill the configuration values

const Configuration = {
    host: "localhost",
    port: 3000,
    db: {
        host: "localhost:27017",
        name: "database",
        user: "user",
        password: "password"
    },
}

export default Configuration;
